export enum CampaignType {
  promotion = 'promotion',
  reward = 'reward',
}

export enum CampaignScope {
  product = 'product',
  cart = 'cart',
}

export enum DiscountType {
  percentage = 'percentage',
  fixed = 'fixed',
}
